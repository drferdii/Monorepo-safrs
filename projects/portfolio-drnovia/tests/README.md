# Portfolio Dr. Novia — Test Suite

Node built-in test runner (`node --test`). No Jest, Vitest, or Playwright setup is required for this capsule.

## Run tests

From Monorepo root:

```bash
node --test projects/portfolio-drnovia/tests/capsule-paths.test.mjs projects/portfolio-drnovia/tests/lenis-contract.test.mjs
```

Or from `projects/portfolio-drnovia`:

```bash
npm test
```
