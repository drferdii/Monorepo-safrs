# PROGRESS — Pointer Only

**This file holds no status. Do not write status here.**

Status lives in `docs/plans/active/`. Every plan owns its own checkboxes, and that is the only
place they exist. A board that copies them is a second copy to forget, and forgetting is exactly
what happened: this file spent a week claiming work was in progress that had never started and
work was pending that had already merged.

- Session state: `HANDOFF.md`. Durable decisions: `DECISIONS.md`.
- The old area board, including the 22-to-14 project migration mapping, is in this file's git
  history at `8f0f84d`. Nothing was lost; it is simply not maintained as a live claim any more.
- When a migration actually starts, its plan gets written then — not a document about work nobody
  has begun.
