# Architecture Decision Records

ADRs use the lifecycle `PROPOSED → ACCEPTED → SUPERSEDED` or `REJECTED`.

Minimum fields: ID, status, date, decision owners, context, options considered, decision, consequences, and supersession link when applicable. File names should be `NNNN-short-title.md`.
