# Operator Scripts

Human and CI entry points live here. `safrs-verify.sh` is canonical; `safrs-verify.ps1` is the Windows adapter. Enforcement logic remains in `tools/safrs/` so entry points stay thin.
