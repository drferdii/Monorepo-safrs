# Shared Packages

Use `packages/<package>/` only for reusable, product-neutral capabilities with a clear owner and real consumers. Each package must document its public interface, compatibility policy, tests, and owning project/team. Changes are R2 because they may affect multiple project capsules.
